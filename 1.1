#include <iostream>

using namespace std;

class matrix {
private:
    int a11, a12, a21, a22;

    int trace() const {
        return a11 + a22;
    }
public:
    int trace() const {
        return a11 + a22;
    }

    matrix(int b11 = 0, int b12 = 0, int b21 = 0, int b22 = 0) : a11(b11), a12(b12), a21(b21), a22(b22) {}

    int operator+() const{
        return trace();
    }

    int operator-() const{
        return -trace();
    }

    bool operator<(const matrix& other) const {
        return +*this < +other;
    }
};

int main()
{
    matrix m(1, 1, 1, 1);
    matrix m1(2, 2, 2, 2);

    cout << (m < m1);
    return 0;
}
